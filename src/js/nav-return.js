/**
 * @deprecated Load src/js/core/nav-return.js instead.
 * Kept so older cached HTML that still points here does not crash.
 */
(function (global) {
  "use strict";
  if (global.JTG && global.JTG.NavReturn) return;
})(window);
