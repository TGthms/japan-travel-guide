# Legacy Flask gallery manager

This folder is **legacy**.

Use the new zero-dependency manager instead:

```bash
# from project root
python3 tools/gallery_manager.py
# or double-click Add Photos.command
```

See `../README.md`.
